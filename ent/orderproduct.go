// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/avored/go-ecommerce/ent/order"
	"github.com/avored/go-ecommerce/ent/orderproduct"
	"github.com/avored/go-ecommerce/ent/product"
)

// OrderProduct is the model entity for the OrderProduct schema.
type OrderProduct struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// OrderID holds the value of the "order_id" field.
	OrderID int `json:"order_id,omitempty"`
	// ProductID holds the value of the "product_id" field.
	ProductID int `json:"product_id,omitempty"`
	// Qty holds the value of the "qty" field.
	Qty float64 `json:"qty,omitempty"`
	// Amount holds the value of the "amount" field.
	Amount float64 `json:"amount,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OrderProductQuery when eager-loading is set.
	Edges OrderProductEdges `json:"edges"`
}

// OrderProductEdges holds the relations/edges for other nodes in the graph.
type OrderProductEdges struct {
	// OrderProductOrder holds the value of the order_product_order edge.
	OrderProductOrder *Order `json:"order_product_order,omitempty"`
	// OrderProductProduct holds the value of the order_product_product edge.
	OrderProductProduct *Product `json:"order_product_product,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// OrderProductOrderOrErr returns the OrderProductOrder value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OrderProductEdges) OrderProductOrderOrErr() (*Order, error) {
	if e.loadedTypes[0] {
		if e.OrderProductOrder == nil {
			// The edge order_product_order was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: order.Label}
		}
		return e.OrderProductOrder, nil
	}
	return nil, &NotLoadedError{edge: "order_product_order"}
}

// OrderProductProductOrErr returns the OrderProductProduct value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OrderProductEdges) OrderProductProductOrErr() (*Product, error) {
	if e.loadedTypes[1] {
		if e.OrderProductProduct == nil {
			// The edge order_product_product was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: product.Label}
		}
		return e.OrderProductProduct, nil
	}
	return nil, &NotLoadedError{edge: "order_product_product"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OrderProduct) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case orderproduct.FieldQty, orderproduct.FieldAmount:
			values[i] = new(sql.NullFloat64)
		case orderproduct.FieldID, orderproduct.FieldOrderID, orderproduct.FieldProductID:
			values[i] = new(sql.NullInt64)
		case orderproduct.FieldCreatedAt, orderproduct.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type OrderProduct", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OrderProduct fields.
func (op *OrderProduct) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case orderproduct.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			op.ID = int(value.Int64)
		case orderproduct.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				op.CreatedAt = value.Time
			}
		case orderproduct.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				op.UpdatedAt = value.Time
			}
		case orderproduct.FieldOrderID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field order_id", values[i])
			} else if value.Valid {
				op.OrderID = int(value.Int64)
			}
		case orderproduct.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				op.ProductID = int(value.Int64)
			}
		case orderproduct.FieldQty:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field qty", values[i])
			} else if value.Valid {
				op.Qty = value.Float64
			}
		case orderproduct.FieldAmount:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value.Valid {
				op.Amount = value.Float64
			}
		}
	}
	return nil
}

// QueryOrderProductOrder queries the "order_product_order" edge of the OrderProduct entity.
func (op *OrderProduct) QueryOrderProductOrder() *OrderQuery {
	return (&OrderProductClient{config: op.config}).QueryOrderProductOrder(op)
}

// QueryOrderProductProduct queries the "order_product_product" edge of the OrderProduct entity.
func (op *OrderProduct) QueryOrderProductProduct() *ProductQuery {
	return (&OrderProductClient{config: op.config}).QueryOrderProductProduct(op)
}

// Update returns a builder for updating this OrderProduct.
// Note that you need to call OrderProduct.Unwrap() before calling this method if this OrderProduct
// was returned from a transaction, and the transaction was committed or rolled back.
func (op *OrderProduct) Update() *OrderProductUpdateOne {
	return (&OrderProductClient{config: op.config}).UpdateOne(op)
}

// Unwrap unwraps the OrderProduct entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (op *OrderProduct) Unwrap() *OrderProduct {
	tx, ok := op.config.driver.(*txDriver)
	if !ok {
		panic("ent: OrderProduct is not a transactional entity")
	}
	op.config.driver = tx.drv
	return op
}

// String implements the fmt.Stringer.
func (op *OrderProduct) String() string {
	var builder strings.Builder
	builder.WriteString("OrderProduct(")
	builder.WriteString(fmt.Sprintf("id=%v", op.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(op.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(op.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", order_id=")
	builder.WriteString(fmt.Sprintf("%v", op.OrderID))
	builder.WriteString(", product_id=")
	builder.WriteString(fmt.Sprintf("%v", op.ProductID))
	builder.WriteString(", qty=")
	builder.WriteString(fmt.Sprintf("%v", op.Qty))
	builder.WriteString(", amount=")
	builder.WriteString(fmt.Sprintf("%v", op.Amount))
	builder.WriteByte(')')
	return builder.String()
}

// OrderProducts is a parsable slice of OrderProduct.
type OrderProducts []*OrderProduct

func (op OrderProducts) config(cfg config) {
	for _i := range op {
		op[_i].config = cfg
	}
}
